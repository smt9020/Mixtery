<h1>Mixtery</h1>

<ul>
  <% @tracks_hash.each do |name, mocks| %>
  <li>
    <%= name %>
    </br>
    <audio controls>
      <source src ="/assets/<%= name %>.mp3" type="audio/mpeg">
    </audio>
  </br>
</br>
    <% mocks.each do |mock| %>
      <input type="radio" value="<%= mock.name %>"><%= mock.name %><br>
      </br>
    <% end %>
  </li>
  <% end %>
</ul>

<br>

<%= link_to 'New Track', new_track_path %>
